import react, {useState} from 'react'

function AddUserModal(){
    return(
        <div>
            <h2>Add new user</h2>
            {/* <form onSubmit={handleSubmit}>
                <input type="text" name="username" placeholder="Username" value={newUser.username} onChange={handleChange} required />
                <input type="number" name="age" placeholder="Age" value={newUser.age} onChange={handleChange} required />
                <input type="number" name="weight" placeholder="Weight" value={newUser.weight} onChange={handleChange} required />
                <input type="number" name="height" placeholder="Height" value={newUser.height} onChange={handleChange} required />
                <button type="submit">Add User</button>
            </form> */}
        </div>
    )
}

export default AddUserModal